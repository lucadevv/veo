# audit-service · secretos DEV (PLANTILLA). Copiar a `development.secret.env` (gitignored) y completar.
# En dev local los defaults del env.schema bastan; este archivo solo si querés sobreescribir un secreto.
# AUDIT_S3_SECRET_KEY = credencial del storage WORM inmutable del audit log (Ley 29733, Object Lock).
# En prod viene de Secrets Manager (fail-fast si arranca con el secreto de dev conocido).
INTERNAL_IDENTITY_SECRET=dev-internal-secret-change-me
AUDIT_S3_SECRET_KEY=veo_dev_secret
