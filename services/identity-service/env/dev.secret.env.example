# identity-service · secretos DEV (PLANTILLA tracked). Copiar a `dev.secret.env` (GITIGNORED) y completar.
# El boot del passenger-stack (dev-stack/boot-passenger-stack.sh) genera el dev.secret.env real
# con un keypair ES256 (EC P-256) PERSISTENTE y consistente con el public-bff.
#
# JWT ES256: privada PKCS#8 + pública SPKI. La MISMA pública va en public-bff (VEO_JWT_PUBLIC_PEM).
# Si faltan, identity genera un par EFÍMERO en cada arranque → el BFF valida con otro → 401. NO dejar vacío en uso real.
JWT_PRIVATE_KEY_PEM=
JWT_PUBLIC_KEY_PEM=
# HMAC de identidad interna. DEBE ser idéntico en identity + places + public-bff.
INTERNAL_IDENTITY_SECRET=
