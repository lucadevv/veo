# Identity · secretos del correo LIVE — TEMPLATE (tracked, sin valores).
# Copiá a `email-live.secret.env` (gitignored) y poné el valor real.
# SMTP_PASS = API key de Resend.com (formato re_...). Rotar al cierre del proyecto.
SMTP_PASS=
