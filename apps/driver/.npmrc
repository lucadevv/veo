# React Native + autolinking requieren un node_modules plano.
# pnpm con node-linker=hoisted replica el layout de npm/yarn que espera el RN CLI.
node-linker=hoisted
# Permite que binarios y módulos nativos transitivos sean accesibles en la raíz.
shamefully-hoist=true
