/**
 * Re-exporta las primitivas de movimiento CANÓNICAS del design system (`@veo/ui-kit`). Una sola
 * fuente; este archivo era una copia idéntica. Las pantallas siguen importando desde aquí.
 */
export {Appear, PressableScale} from '@veo/ui-kit';
