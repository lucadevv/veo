## Template para Deployment + Service de un microservicio VEO.
## Copiar a services/<svc-name>.yaml y sustituir public-bff y 4001.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: public-bff
  labels: { app: public-bff, tier: bff }
spec:
  replicas: 2
  strategy: { type: RollingUpdate, rollingUpdate: { maxSurge: 1, maxUnavailable: 0 } }
  selector: { matchLabels: { app: public-bff } }
  template:
    metadata:
      labels: { app: public-bff, tier: bff }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "4001"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: public-bff  # IRSA bound to least-privilege IAM role
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector: { matchLabels: { app: public-bff } }
      containers:
        - name: app
          image: ecr/public-bff:latest
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 4001, protocol: TCP }
          env:
            - { name: NODE_ENV, value: production }
            - { name: PORT, value: "4001" }
          envFrom:
            - secretRef: { name: public-bff-secrets }
            - configMapRef: { name: public-bff-config }
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "1000m", memory: "1Gi" }
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health/ready, port: http }
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          startupProbe:
            httpGet: { path: /health, port: http }
            failureThreshold: 30
            periodSeconds: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector: { matchLabels: { app: public-bff } }
---
apiVersion: v1
kind: Service
metadata:
  name: public-bff
  labels: { app: public-bff }
spec:
  type: ClusterIP
  ports:
    - { name: http, port: 80, targetPort: http, protocol: TCP }
  selector: { app: public-bff }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: public-bff-pdb
spec:
  minAvailable: 1
  selector: { matchLabels: { app: public-bff } }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: public-bff-hpa
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: public-bff }
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
    - type: Resource
      resource: { name: memory, target: { type: Utilization, averageUtilization: 80 } }
