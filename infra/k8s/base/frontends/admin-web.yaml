## Template para Deployment + Service de un microservicio VEO.
## Copiar a services/<svc-name>.yaml y sustituir admin-web y 5000.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-web
  labels: { app: admin-web, tier: frontend }
spec:
  replicas: 2
  strategy: { type: RollingUpdate, rollingUpdate: { maxSurge: 1, maxUnavailable: 0 } }
  selector: { matchLabels: { app: admin-web } }
  template:
    metadata:
      labels: { app: admin-web, tier: frontend }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5000"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: admin-web  # IRSA bound to least-privilege IAM role
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector: { matchLabels: { app: admin-web } }
      containers:
        - name: app
          image: ecr/admin-web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 5000, protocol: TCP }
          env:
            - { name: NODE_ENV, value: production }
            - { name: PORT, value: "5000" }
          envFrom:
            - secretRef: { name: admin-web-secrets }
            - configMapRef: { name: admin-web-config }
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits:   { cpu: "1000m", memory: "1Gi" }
          livenessProbe:
            httpGet: { path: /api/health, port: http }
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /api/health, port: http }
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          startupProbe:
            httpGet: { path: /api/health, port: http }
            failureThreshold: 30
            periodSeconds: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector: { matchLabels: { app: admin-web } }
---
apiVersion: v1
kind: Service
metadata:
  name: admin-web
  labels: { app: admin-web }
spec:
  type: ClusterIP
  ports:
    - { name: http, port: 80, targetPort: http, protocol: TCP }
  selector: { app: admin-web }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: admin-web-pdb
spec:
  minAvailable: 1
  selector: { matchLabels: { app: admin-web } }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: admin-web-hpa
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: admin-web }
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
    - type: Resource
      resource: { name: memory, target: { type: Utilization, averageUtilization: 80 } }
