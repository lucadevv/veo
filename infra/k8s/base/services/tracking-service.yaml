## Template para Deployment + Service de un microservicio VEO.
## Copiar a services/<svc-name>.yaml y sustituir tracking-service y 3004.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-service
  labels: { app: tracking-service, tier: backend }
spec:
  replicas: 2
  strategy: { type: RollingUpdate, rollingUpdate: { maxSurge: 1, maxUnavailable: 0 } }
  selector: { matchLabels: { app: tracking-service } }
  template:
    metadata:
      labels: { app: tracking-service, tier: backend }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "3004"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: tracking-service # IRSA bound to least-privilege IAM role
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile: { type: RuntimeDefault }
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector: { matchLabels: { app: tracking-service } }
      containers:
        - name: app
          image: ecr/tracking-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - { name: http, containerPort: 3004, protocol: TCP }
          env:
            - { name: NODE_ENV, value: production }
            - { name: TRACKING_HTTP_ADDR, value: ":3004" }
          envFrom:
            - secretRef: { name: tracking-service-secrets }
            - configMapRef: { name: tracking-service-config }
          resources:
            requests: { cpu: "100m", memory: "256Mi" }
            limits: { cpu: "1000m", memory: "1Gi" }
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health/ready, port: http }
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          startupProbe:
            httpGet: { path: /health, port: http }
            failureThreshold: 30
            periodSeconds: 2
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: kubernetes.io/hostname
                labelSelector: { matchLabels: { app: tracking-service } }
---
apiVersion: v1
kind: Service
metadata:
  name: tracking-service
  labels: { app: tracking-service }
spec:
  type: ClusterIP
  ports:
    - { name: http, port: 80, targetPort: http, protocol: TCP }
  selector: { app: tracking-service }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: tracking-service-pdb
spec:
  minAvailable: 1
  selector: { matchLabels: { app: tracking-service } }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: tracking-service-hpa
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: tracking-service }
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
    - type: Resource
      resource: { name: memory, target: { type: Utilization, averageUtilization: 80 } }
